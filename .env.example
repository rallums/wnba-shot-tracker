# Copy to .env.local — never commit .env.local

# Vercel KV (get from Vercel dashboard after adding KV store)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Secret for Monday cron job (generate any random string)
CRON_SECRET=your-secret-here
